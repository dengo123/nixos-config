{
  config,
  lib,
  osConfig,
  namespace,
  ...
}:
with lib;
with lib.${namespace}; {
  snowfallorg.user.enable = true;

  ${namespace} = {
    bundles = {
      common = enabled;
      # Add optional bundles below
      # desktop.hyprland = enabled;
      # desktop.awesome = enabled;
      # developer.editor = "doom";
      # office = enabled;
    };

    programs = {
        git.username = "your-username"; # TODO: Change me!
      };
      # Add more programs here
    };

    misc = {
      xdg = enabled;
    };
  };

  home.stateVersion = lib.mkDefault (osConfig.system.stateVersion or "24.05");
}
