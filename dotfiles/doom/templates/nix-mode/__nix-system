{
  lib,
  namespace,
  ...
}:
with lib;
with lib.${namespace};
{
  imports = [ ./hardware.nix ];

  networking.hostName = "your-hostname"; # TODO: Change me!

  ${namespace} = {
    config = {
      user = {
        name = "your-username"; # TODO: Change me!
        extraGroups = [
          "networkmanager"
          "wheel"
        ];
      };
    };

    bundles = {
      common = enabled;
    };

    # desktop.hyprland = enabled; # TODO: Set a desktop environment
    # desktop.session = enabled;
    # desktop.kde = enabled;

    hardware = {
      bluetooth = enabled;
      audio = enabled;
      # cuda = enabled;
      # nvidia = enabled;
    };

    programs = {
      steam = enabled;
    };

    system = {
      boot.grub = enabled;
      # boot.systemd = enabled;
      keyboard = {
        layout = "us";
        variant = "intl";
      };
      region = {
        locale = "de_DE.UTF-8";
        timeZone = "UTC";
      };
    };
  };

  system.stateVersion = "24.05";
}
