;;; packages.el -*- lexical-binding: t; -*-

;; Zusätzliche Pakete hier deklarieren. Beispiele:
;; (package! beacon)
;; (package! catppuccin-theme)

;; Wenn du ein Paket brauchst, das nicht in nixpkgs/emacs-overlay ist:
;; (package! foo :recipe (:host github :repo "user/foo"))

